package snakeAndLadder;

public class Player {
    int playerId;
    char piece;
    int curPos;

    public Player(int playerId, char piece, int curPos) {
        this.playerId = playerId;
        this.piece = piece;
        this.curPos = curPos;
    }

}
